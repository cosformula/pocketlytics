# Public URL for users
BASE_URL=https://analytics.example.com

# Hostname only (used by Traefik routing rule)
RYBBIT_HOST=analytics.example.com

# Required auth secret (use a long random string)
BETTER_AUTH_SECRET=replace_with_a_long_random_secret

# App behavior
DISABLE_SIGNUP=false
DISABLE_TELEMETRY=true

# Embedded DB files in container volume
SQLITE_DB_PATH=file:/app/data/rybbit.sqlite
DUCKDB_PATH=/app/data/rybbit-analytics.duckdb

# Optional tokens
MAPBOX_TOKEN=
